import { ServiceContainer } from "@xieyuheng/enchanter/lib/service-container"
import { BookState } from "@/views/book/book-state"

export class App extends ServiceContainer {
  // async buildBookState(): Promise<BookState> {

  // }
}
